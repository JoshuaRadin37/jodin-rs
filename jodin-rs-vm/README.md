# File Layout
```
MAGIC_NUM
public{
    <id>                    <type>
    main                    fn() -> int
    std::math::fib          fn(int) -> int
    vector                  template<T>
    
}
```